# Software Fault Tolerance Scheduling techniques

## Building

Requirements:

- Lua (5.3 or above, a interpreter source code is included in `vendor` for convenience)
- Clang (hosted build) or ARM embedded GCC toolchain (stm32 build). Both of these compilers must support C++20

Run `lua build.lua` in the `src` directory and you'll get the help required to build

